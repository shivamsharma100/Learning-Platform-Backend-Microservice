<?xml version="1.0" encoding="UTF-8"?>
<databaseChangeLog
        xmlns="http://www.liquibase.org/xml/ns/dbchangelog"
        xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
        xsi:schemaLocation="
            http://www.liquibase.org/xml/ns/dbchangelog
            http://www.liquibase.org/xml/ns/dbchangelog/dbchangelog-4.23.xsd">

    <changeSet id="001-create-enrollment-table" author="shivam">

        <!-- Create Enrollment Table -->
        <createTable tableName="enrollments">
            <column name="id" type="INT" autoIncrement="true">
                <constraints primaryKey="true" nullable="false"/>
            </column>

            <column name="course_id" type="INT">
                <constraints nullable="false"/>
            </column>

            <column name="learner_id" type="INT">
                <constraints nullable="false"/>
            </column>

            <column name="status" type="VARCHAR(50)">
                <constraints nullable="false"/>
            </column>

            <column name="enrolled_at" type="TIMESTAMP">
                <constraints nullable="false"/>
            </column>
        </createTable>

        <!-- Foreign Key to Course -->
        <addForeignKeyConstraint
                constraintName="fk_enrollment_course"
                baseTableName="enrollments"
                baseColumnNames="course_id"
                referencedTableName="courses"
                referencedColumnNames="id"
                onDelete="CASCADE"/>

        <!-- Prevent duplicate enrollment -->
        <addUniqueConstraint
                tableName="enrollments"
                columnNames="course_id, learner_id"
                constraintName="uk_course_learner"/>

        <!-- Indexes -->
        <createIndex tableName="enrollments" indexName="idx_enrollment_course">
            <column name="course_id"/>
        </createIndex>

        <createIndex tableName="enrollments" indexName="idx_enrollment_learner">
            <column name="learner_id"/>
        </createIndex>

    </changeSet>

</databaseChangeLog>
